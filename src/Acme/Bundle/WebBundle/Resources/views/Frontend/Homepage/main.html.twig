{% extends 'AcmeWebBundle:Frontend:layout.html.twig' %}

{% block slideshow_full %}
    <div class="row">
        {# {{ sonata_block_render({'name': '/cms/blocks/slideshow-full'}) }} #}
    </div>
{% endblock %}

{% block slideshow_sidebar %}
    <div class="row">
        {# {{ sonata_block_render({'name': '/cms/blocks/slideshow-sidebar'}) }} #}
    </div>
{% endblock %}


{% block content %}
{# 
<div class="jumbotron">
    <h1>{{ 'sylius.homepage.splash.headline'|trans }}</h1>
    <p class="lead">{{ 'sylius.homepage.splash.subheadline'|trans }}</p>
</div>
 #}
{# {{ sonata_block_render({'name': '/cms/blocks/slideshow-home'}) }} #}

<p>Homepage Main action!</p>
{{ render( url( 'acme_partial_product_latest', {'limit': 20, 'template': 'AcmeWebBundle:Frontend/Product:latest.html.twig'} )) }}
{# {{ render(url('acme_partial_product_latest')) }} #}
{# {{ render( controller( 'acme.controller.frontend.homepage:indexAction', {'limit': 9, 'template': 'AcmeWebBundle:Frontend/Product:latest.html.twig'} )) }}  #}

{% endblock %}
